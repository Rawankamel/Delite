<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fci.delite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home" />
        <activity android:name=".Menu" />
        <activity android:name=".Order" />
        <activity android:name=".menu_jc" />
        <activity android:name=".Menu_desrt" />
        <activity android:name=".OrderSignup" />
        <activity android:name=".OrderOrders" />
        <activity android:name=".OrderConfirmation" />
        <activity android:name=".Main2" />
        <activity android:name=".HFH" />
        <activity
            android:name=".QuickNav"
            android:label="@string/title_activity_quick_nav"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>